# Codex Adapter

- Install the committed skill through the approved Codex skill installer or deployment workflow.
- Keep shared `SKILL.md` frontmatter limited to `name` and `description`.
- Keep `agents/openai.yaml` synchronized with the canonical name and trigger.
- Confirm the actual callable media app, MCP, API wrapper, local binary, or connected provider; a named provider is not automatically available.
- Invoke `z-audio-production` for production audio and validate its handoff before visual work.
- Run both skill validators and test discovery and trigger boundaries in a fresh task.
- Treat GitHub as the authoring source and local installed folders as replaceable deployment copies.

