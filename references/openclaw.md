# OpenClaw Adapter

- Inspect active skill roots, loading precedence, agent workspace, video tools, provider credentials, FFmpeg, Remotion project, and filesystem ownership before deployment.
- Verify provider model discovery and schema independently from the reasoning-model authentication.
- Use the organization Skill Workshop workflow when enabled and preserve mandatory review enforcement.
- Keep long-running provider jobs idempotent: record job IDs, poll status, and avoid duplicate paid submissions.
- Use only authorized governed sub-agents; pass a locked brief and scene manifest, and keep one director responsible for cost and approval.
- Restart the gateway or use a fresh session when skill metadata is cached.
- Test one agent and a small controlled proof before broader rollout.

